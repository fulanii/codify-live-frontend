import { PagePlaceholder } from '../components/PagePlaceholder';

export function CallsPage(): JSX.Element {
  return (
    <PagePlaceholder
      title="Calls"
      description="Peer-to-peer audio and video over WebRTC, with the backend acting as a signaling relay only."
      icon="call"
    />
  );
}
